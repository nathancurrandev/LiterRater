/** @type {import('next').NextConfig} */
const nextConfig = {
  // API-only Next.js project — no frontend pages, no SSR
  // All routes live under pages/api/ only
  reactStrictMode: false,
};

module.exports = nextConfig;
